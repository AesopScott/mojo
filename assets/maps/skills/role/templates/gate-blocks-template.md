# Gate Blocks

Status: no open gate blocks.

## Purpose

Track active gate blocks for [proper-role-name].

When this role is blocked by the Codex tool gate, add one entry under `Open Blocks` with time, blocked action, target path or command, approval needed, and current owner.

When the gate clears, remove the entry from `Open Blocks` and add a short note under `Cleared Blocks`.

## Open Blocks

None.

## Cleared Blocks

None.
